// command line

/ root
~ user / home
. current location
..

ls -options (- l,a)
pwd
cd
mkdir
touch
mv
cp

rm
rm -r
rm -rf

chmod

clear
man

open?
open -a "application name"

// git

git clone
git add
git commit -m "message"
git push
git pull
git status
git brach
git checkout
git merge

// NPM

npm init
npm install -g
npm run
npm cache clean --force
npm update -g
npm uninstall
npm ls -g

* - latest version

// express

express nameproject

// jade
extends
block

() - attribute
  () expression
#{} - print variable
element= print variable


// mongodb
mongo - enter the envi mongodb

show dbs
use nameDatabase

show collection
db.createcollection('name');

// insert data to collection
db.nameCollection.insert({ key: value });

// update with set
db.nameCollection.update(
  { conditional },
  {
    $set: {
      key: value
    }
  }
)

// eslint
--init

// libraries
toastr