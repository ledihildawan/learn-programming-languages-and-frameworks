// Use meaningful variable names
// Variable names should be memorable
// Favor descriptive names over short ones
// Avoid unnecessary context
// Use let instead of var
// Variable declared with let are not part of the global object

// What is the different between function scope and block scope?
// Where does the JavaScript engine look? // In the current scope and all enclosing scopes.

// Always favor strict mode
// Single responsibility
// Reduce number of arguments
// Avoid flags
// Avoid callbacks
// Use Object.assign or the Spread Operator for Default Arguments
// Function definition does not metter
// From where the function is called metters
// call and bind
// .bind() does not call the function
// What about this?

// Use property descriptors to create read only fields
// Favor ES6 inheritance over prototypal inheritance
// Use Object.fre eze instead of const